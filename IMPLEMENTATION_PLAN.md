# Tera High 実装引き継ぎ計画 — ケイサンの壁 学習プラットフォーム化

## 0. この文書の目的

本書は、現行の静的 PWA「ケイサンの壁」を、小学4年生から中学1年生までが継続して学べるシンプルな学習ゲームへ拡張するための実装計画である。

この段階では実装しない。Tera High は、本書のフェーズ順・受け入れ条件・非機能要件に従って実装すること。仕様に曖昧さがある場合は「操作の簡単さ」「学習としての正しさ」「既存データの保持」の順に優先する。

---

## 1. 現状分析

### 1.1 現在の技術構成

- Vanilla HTML / CSS / JavaScript の単一ページアプリ
- PWA 対応（`manifest.webmanifest`、`sw.js`）
- サーバー・ビルド工程・外部依存なし
- Cookie に XP、メダル、ランキング、サウンド設定を保存
- スマートフォン縦画面を中心に、タブレット・PCにも対応
- Web Audio API による効果音

### 1.2 現在の学習機能

- 小学生向け掛け算・割り算 3難易度
- 中学生向け正負の数・計算順序・累乗 3難易度
- 百人一首 10枚 / 100枚モード
- 10問タイムアタック
- 連続正解、XP、称号、メダル、ベスト10、紙吹雪
- 3回誤答時の答え表示

### 1.3 維持すべき強み

- タップ数が少なく、説明を読まなくても遊べる
- 大きな問題表示とテンキー
- オフラインでも動作する
- 既存のキャラクター、効果音、コンボ演出
- 子どもの端末内だけで完結し、アカウント登録を要求しない

### 1.4 解消すべき課題

- 「小学生」「中学生」の粒度が粗く、小4〜中1の学習段階が見えない
- 教科・モードが増えると、現在のホーム画面構造では縦長になりやすい
- 記録はタイム中心で、苦手問題の復習導線がない
- Cookie は容量制限があり、問題別履歴の保存に向かない
- 問題データ・画面制御・保存処理が `script.js` に集中している
- 10問固定の進捗 UI は100問モードに流用できない
- 正解を教えるだけで、解き方の理解確認まではできない

---

## 2. プロダクト方針

### 2.1 コア体験

1. 学年を一度選ぶ
2. 教科カードを1回タップする
3. 10秒以内に最初の問題へ到達する
4. 1プレイは通常2〜5分で完了する
5. 終了時に「成長」「報酬」「次にやること」を1画面で示す

### 2.2 設計原則

- ホームには「学年」「今日の目標」「教科」の3要素だけを置く
- 通常プレイは「問題を見る → 1操作で答える → 次へ」を繰り返す
- 長い説明はプレイ前に読ませず、誤答後に短く出す
- 速さだけでなく正確さと復習を同等に評価する
- 学年は出題範囲の初期値であり、他学年の教材もロックしない
- 課金、広告、対人チャット、公開ランキングは入れない
- 赤色だけに頼らず、記号・文言・音でも正誤を伝える
- `prefers-reduced-motion` とサウンドOFFを全モードで尊重する

### 2.3 成功指標

初期リリース後に端末内で集計可能な範囲に限定する。

- 初回起動から最初の回答まで60秒以内
- ホームから問題開始まで最大3タップ
- 1プレイ完了率 80%以上を目標
- 誤答問題の復習実行率 30%以上を目標
- 7日間で3日以上遊ぶユーザー比率を継続指標とする
- 主要画面で 360 × 640 px に横スクロールが発生しない

外部 analytics は初期リリースに含めない。数値は `localStorage` の匿名端末内統計で確認できる開発者向けデバッグ表示として実装する。

---

## 3. 情報設計と画面遷移

### 3.1 画面構成

```text
起動
└─ 初回のみ学年選択
   └─ ホーム
      ├─ 学年切替（小4 / 小5 / 小6 / 中1）
      ├─ 今日のミッション
      ├─ 算数・数学
      │  └─ コース選択 → 問題 → 結果 → 復習
      ├─ 100マス
      │  └─ 種類選択 → 問題 → 結果 → 復習
      ├─ 英語
      │  └─ 種類選択 → 問題 → 結果 → 復習
      ├─ 国語・百人一首
      │  └─ 既存の枚数選択 → 問題 → 結果
      └─ きろく
         ├─ レベル・XP・コイン
         ├─ 学習カレンダー
         ├─ 教科別の星
         └─ ベスト記録
```

### 3.2 ホーム画面

- 上段：現在の学年、連続学習日数、サウンド
- 中段：今日のミッション1件と進捗
- 下段：4つの大きな教科カード
  - 算数・数学
  - 100マス
  - 英語
  - 百人一首
- 最下部：「きろく」ボタン
- キャラクターは装飾兼リアクション役とし、ナビゲーションには使わない

### 3.3 共通プレイ画面

- 上：残り問題数または進捗率、タイム、コンボ
- 中：問題、必要時のみ短い補助文
- 下：回答 UI
- 画面下端：消す / やり直す / ホーム
- 10問モードはドット、20問以上はプログレスバーを使用
- 結果画面は「正解数」「正答率」「タイム」「獲得XP」「ミッション進捗」「復習する」を表示

---

## 4. 学習コンテンツ設計

## 4.1 算数・数学

各学年3コースを基本とし、1セット10問。「れんしゅう」は時間を強調せずヒントあり、「チャレンジ」はタイム計測ありとする。

| 学年 | コースA | コースB | コースC |
|---|---|---|---|
| 小4 | 大きな数の四則計算 | 小数の足し算・引き算 | 同分母の分数 |
| 小5 | 小数の掛け算・割り算 | 分数の足し算・引き算 | 割合・単位量あたり |
| 小6 | 分数の掛け算・割り算 | 比・比例 | 文字を使った式の準備 |
| 中1 | 正負の数 | 文字式 | 一次方程式 |

#### 問題形式

- 整数：既存テンキー
- 小数：テンキーへ小数点を追加
- 分数：分子・分母を別フィールドで入力。約分前の同値分数も正解扱い
- 割合・比：4択と数値入力を問題に応じて切り替える
- 文字式：4択または係数入力
- 方程式：`x =` の右側だけを数値入力

#### 問題生成のルール

- 学年・コースごとに値域と出題パターンを定義ファイルへ分離する
- 0除算、極端に大きい答え、意図しない循環小数は生成しない
- 分数は内部的に `{ numerator, denominator }` で扱い、比較前に最大公約数で正規化する
- 同じ問題を1セット内で重複させない
- 直近3セットで頻出した問題と完全一致する問題を避ける
- 出題パターンごとに自動テストを作り、1,000回生成して不正値がないことを確認する

#### 誤答フィードバック

- 1回目：「もう一度」のみ
- 2回目：考え方のヒントを1行表示
- 3回目：答えと最短の解説を表示し、「わかった」を押して次へ
- 誤答した問題は苦手キューへ追加し、セット終了後の復習へ回す

## 4.2 100マス計算

### 初期リリース範囲

- 足し算
- 引き算
- 掛け算
- 10マス（練習）
- 50マス（標準）
- 100マス（本番）

割り算は答えが整数になる問題だけに制限し、初期リリース後の追加候補とする。

### 操作設計

- 10 × 10 の表を表示し、現在のマスだけ強調する
- スマートフォンでは表全体を縮小表示し、問題文を上に大きく再掲する
- 回答は既存テンキーを再利用する
- 正解で自動的に次のマスへ進む
- 誤答時に次へ進めず、2回目で正答を表示する設定を選べる
- 「正確さ優先」を初期値とし、タイムは結果画面でのみ大きく表示する
- 中断時は現在位置を保存し、ホームに「つづきから」を出す

### 難易度

- 小4：0〜9を中心に四則の基礎
- 小5：足し引きに2桁を混ぜる
- 小6：負数なし、暗算可能な小数を一部追加
- 中1：正負の数を選択可能

### 評価

- 正答率100%かつ自己ベスト更新で特別バッジ
- タイム評価は過去の自分との比較を主とする
- 一律の厳しい基準タイムで劣等感を与えない

## 4.3 英語

### 初期リリース範囲

| 学年 | 主な内容 | 初期問題数の目安 |
|---|---|---:|
| 小4 | 色、数、曜日、天気、身近な物、基本あいさつ | 80 |
| 小5 | 月、時刻、教科、食べ物、できること、`I like ...` | 120 |
| 小6 | 過去・将来の入口、行きたい国、職業、日常表現 | 140 |
| 中1 | be動詞、一般動詞、疑問文、否定文、代名詞、基本語順 | 180 |

問題数は語彙と例文を合計した最小目標。内容は教育経験者によるレビューを公開前の必須工程とする。

### モード

1. 英単語：英語を見て日本語4択
2. 日本語から英語：日本語を見て英語4択
3. 文のならべかえ：3〜7語のカードをタップして並べる
4. リスニング：音声を聞いて4択（対応ブラウザのみ）

初期実装は1と2を必須、3を第2段階、4を Progressive Enhancement とする。

### 問題データ形式

```js
{
  id: "en-g4-color-red",
  grades: [4],
  type: "word",
  prompt: "red",
  answer: "赤",
  distractors: ["青", "黄", "緑"],
  example: "I like red.",
  hint: "りんごの色を思い出そう",
  tags: ["color"]
}
```

- `id` はリリース後に変更しない
- 誤答選択肢は同じ品詞・カテゴリから選ぶ
- 大文字小文字、句読点、全角半角の許容ルールを共通化する
- 音声はまず `speechSynthesis` を使い、未対応時はボタンを非表示にする
- 自動音声だけを正しい発音の唯一の教材として扱わない

### 学習サイクル

- 新しい問題 7問 + 復習問題 3問を標準セットとする
- 初回正解：次回候補を3日後へ
- 1回誤答：翌日までに再出題
- 2回以上誤答：同じセットの最後に再出題
- 連続3回正解で「おぼえた」にする
- ユーザーは「もう一度練習」へ手動で戻せる

## 4.4 百人一首

- 現行機能を維持し、初期リリースでは大規模改修しない
- ホーム上は「国語・百人一首」と表示する
- XP・デイリーミッション・共通結果画面へ接続する
- 既存の10枚 / 100枚ランキングとペナルティ計算を保持する
- 後続候補：決まり字練習、作者クイズ、苦手札練習

---

## 5. ゲーミフィケーション設計

ゲーミフィケーションは学習を止める障壁にせず、「始める理由」「続ける手応え」「復習する理由」を作るために使う。

### 5.1 共通報酬

- XP：全教科共通。正解、完了、復習で獲得
- コイン：ミッション達成と初回クリアで獲得
- 星：各コースで正確さ、完了、自己ベストの3条件
- 称号：既存の称号を全教科型へ改名し、段階を維持
- メダル：既存記録を保持し、計算チャレンジ専用実績として残す

### 5.2 XP計算

```text
基本XP = 正解数
完了ボーナス = 5
ノーミス = 5
苦手復習で正解 = 2 / 問
初回クリア = 10
1日の獲得上限 = 設けない
```

速さだけのXPボーナスは付けない。100マスや計算で速度を競う要素は自己ベスト表示に限定する。

### 5.3 デイリーミッション

1日1件だけ表示し、以下から端末内で日付をseedに決定する。

- どれか1セット完了
- 10問連続正解
- 英語を10問練習
- 100マスを10マス以上進める
- 苦手問題を3問復習

未解放モードを要求しない。達成報酬はコイン20枚。日付変更は端末のローカル日付で判定する。

### 5.4 連続学習

- その日に1セット完了すると学習日として記録
- 連続日数はホームに小さく表示
- 途切れても叱責文を出さず「今日からまた1日目！」と表示
- 日付を過去へ戻した場合に連続日数が増えないよう、最終記録日を保持する

### 5.5 コインの使用先

初期リリースではキャラクターの「応援スタンプ」解放だけにする。

- 100 / 200 / 300コインで段階解放
- 能力差・問題スキップなど学習上の有利不利は付けない
- ガチャ、ランダム報酬、期間限定煽りは入れない

### 5.6 演出の強度

- 1〜2連続：通常の正解音
- 3〜4連続：コンボ文言
- 5〜9連続：キャラクター反応
- 10連続：短い紙吹雪
- レベルアップ・自己ベスト：結果画面でのみ大きな演出
- 1問ごとの演出は350ms以内を目安にし、学習テンポを止めない

---

## 6. 学習履歴と適応出題

### 6.1 保存方式

- 設定と既存ランキング：後方互換のため当面 Cookie を読み取る
- 新しいプロフィール・履歴：`localStorage`
- 大規模な回答ログ：初期リリースでは保存しない
- 将来的に履歴が増えた場合のみ IndexedDB を検討する

### 6.2 データモデル

```js
{
  version: 2,
  profile: {
    grade: "g4",
    xp: 0,
    coins: 0,
    titleId: "starter",
    unlockedReactions: []
  },
  activity: {
    lastPlayedDate: "2026-07-17",
    streakDays: 1,
    playedDates: ["2026-07-17"]
  },
  mission: {
    date: "2026-07-17",
    id: "complete-one-set",
    progress: 0,
    claimed: false
  },
  mastery: {
    "en-g4-color-red": {
      attempts: 3,
      correct: 2,
      streak: 1,
      nextReviewAt: "2026-07-18"
    }
  },
  bestScores: {},
  inProgress: null
}
```

### 6.3 マイグレーション

初回起動時に以下を一度だけ実行する。

1. 現在の Cookie から XP・メダル・各ランキング・サウンド設定を読む
2. 新形式へコピーする
3. `version: 2` と `migratedFromCookie: true` を保存する
4. 旧Cookieは削除せず、最低2リリース保持する
5. 保存失敗時は旧形式のまま遊べるようにする

---

## 7. 技術設計

### 7.1 ファイル分割

フレームワーク導入は行わず、ES Modules に段階移行する。

```text
index.html
style.css
src/
├─ app.js                    # 起動・画面遷移
├─ state.js                  # セッション状態
├─ storage.js                # 永続化・マイグレーション
├─ audio.js                  # 効果音
├─ gamification.js           # XP・コイン・ミッション・連続日数
├─ ui/
│  ├─ home.js
│  ├─ play.js
│  ├─ result.js
│  └─ records.js
├─ modes/
│  ├─ calculation.js
│  ├─ hundred-grid.js
│  ├─ english.js
│  └─ karuta.js
├─ content/
│  ├─ math-config.js
│  ├─ english-g4.js
│  ├─ english-g5.js
│  ├─ english-g6.js
│  └─ english-j1.js
└─ utils/
   ├─ random.js
   ├─ fraction.js
   └─ date.js
tests/
├─ math-generators.test.js
├─ fraction.test.js
├─ english-content.test.js
├─ gamification.test.js
└─ storage-migration.test.js
```

`script.js` を一度に全面置換しない。まず純粋関数を抽出し、既存機能の動作確認後に画面単位で移す。

### 7.2 共通モードインターフェース

各モードは最低限、次の契約を満たす。

```js
{
  id,
  createSession(options),
  getCurrentQuestion(session),
  checkAnswer(question, answer),
  getHint(question, attemptCount),
  advance(session, result),
  getSummary(session)
}
```

UIはこのインターフェースだけを参照し、教科固有の正誤判定を持たない。

### 7.3 状態遷移

```text
idle → countdown → answering → feedback → answering/finished
                         └→ paused → answering
finished → review → result/home
```

- 二重タップ・Enter連打で2問進まないよう、状態遷移を単一箇所で管理する
- `setTimeout` はセッションtokenと紐付け、ホームへ戻った後に発火させない
- `requestAnimationFrame` と音声読み上げは画面離脱時に必ず停止する

### 7.4 PWA

- `sw.js` のキャッシュ名をリリースごとに更新する
- 新しい `src/` と英語データを precache 対象へ追加する
- HTML は network-first、静的教材は cache-first を維持する
- 新バージョン検知時に「更新できます」表示を出し、プレイ中は強制更新しない
- オフラインで全必須モードが起動できることを受け入れ条件にする

### 7.5 セキュリティとプライバシー

- 氏名、学校名、メールアドレスを収集しない
- 外部送信を初期リリースでは行わない
- HTMLへ埋め込む可変文字列は必ずエスケープする
- `innerHTML` は静的テンプレートまたはエスケープ済み値だけに限定する
- 将来 analytics を入れる場合は保護者向け説明と同意設計を別途行う

---

## 8. 実装フェーズ

## Phase 0 — ベースライン固定（0.5〜1日）

### 作業

- 現行の全画面を360×640、390×844、768×1024、PCで記録
- 計算6コース、百人一首10枚、ランキング、PWA起動のスモークテストを作る
- ルート版を正とし、`new/` は参照用か不要かを確認する
- 現在の未コミット差分が改行コードだけか確認し、ユーザー変更を上書きしない

### 完了条件

- 既存動作のチェックリストとスクリーンショットが残っている
- 実装前後を比較できる

## Phase 1 — 共通基盤と新ホーム（2〜3日）

### 作業

- 学年選択、小4〜中1プロフィール、教科カードを追加
- `localStorage` v2とCookieマイグレーションを実装
- 共通の画面遷移、共通結果モデル、進捗バーを実装
- XPを共通化し、既存XPを保持
- 既存の計算と百人一首を新ホームから起動可能にする

### 完了条件

- 初回学年選択後、2回目以降はホームへ直行する
- 既存のランキング・XP・メダルが失われない
- 計算と百人一首に回帰不具合がない

## Phase 2 — 100マス MVP（2〜3日）

### 作業

- 10 / 50 / 100マス、足し算・引き算・掛け算を実装
- 表、現在問題の大表示、テンキー、再開データを実装
- 自己ベストと正答率を保存
- 100問用プログレスバーと結果画面を実装

### 完了条件

- 100問すべてに到達でき、重複回答やマス飛びがない
- リロード後に中断位置から再開できる
- 360px幅でテンキーと現在問題が同時に操作できる

## Phase 3 — 英語 MVP（3〜4日 + 教材レビュー）

### 作業

- 学年別データを作成
- 英→日、日→英の4択を実装
- 新規7 + 復習3の出題ロジックを実装
- 苦手キュー、ヒント、例文表示を実装
- 選択肢の重複・正解欠落を検査するcontent testを追加

### 完了条件

- 各学年で最低30問のレビュー済みデータを先行搭載し、後から目標数まで増やせる
- 4つの選択肢が常に一意で、正解が必ず1つ存在する
- 誤答問題が同セット末尾または翌日の復習に出る
- 未レビュー教材を本番へ含めない

## Phase 4 — 学年別算数・数学拡張（4〜6日 + 教材レビュー）

### 作業

- 小数点テンキー、分数入力、4択、並べ替えの共通回答部品を作る
- 学年別12コースを順に実装
- 問題生成テストと短い解説を追加
- 現在の6コースは「旧チャレンジ」として当面アクセス可能にする

### 実装順

1. 小4 大きな数、小数
2. 中1 正負の数、文字式、方程式
3. 小5 小数、分数、割合
4. 小6 分数、比・比例、文字式準備

### 完了条件

- 各generatorの1,000回試行で不正問題が0件
- 分数の同値回答を正解判定できる
- すべての問題に3回目誤答時の解説がある

## Phase 5 — ゲーミフィケーション完成（2〜3日）

### 作業

- デイリーミッション、コイン、学習連続日数、学習カレンダーを実装
- 応援スタンプ解放を実装
- 結果画面で報酬理由を明示
- 日付変更・タイムゾーン・端末時計巻き戻しのテストを追加

### 完了条件

- 同じミッション報酬を1日に2回受け取れない
- 連続日数が時計巻き戻しで増えない
- 報酬演出をスキップしても保存結果が変わらない

## Phase 6 — 品質保証とリリース（2〜3日）

### 作業

- 全モードのキーボード操作・タッチ操作を確認
- 主要4画面サイズ、iOS Safari、Android Chrome、Desktop Chromeで確認
- オフライン、更新、保存容量不足、Cookie無効時を確認
- LighthouseのAccessibility / Best Practices / PWA相当項目を確認
- Service Workerキャッシュを更新
- READMEに開発・検証・データ追加手順を記載

### リリース判定

- P0 / P1バグが0件
- 既存記録の消失が0件
- オフラインで計算・100マス・英語・百人一首が開始可能
- 主要操作にフォーカス表示とアクセシブルネームがある
- 360×640で主要操作が画面外へ隠れない

---

## 9. テスト計画

### 9.1 Unit tests

- 整数・小数・分数・正負・方程式の正誤判定
- 分母0を生成しない
- 四択に重複がない
- Fisher–Yates shuffle が元配列を破壊しない
- XP、コイン、レベルアップ境界
- ミッション日付seedと一日一回報酬
- Cookieからv2へのマイグレーション
- 苦手問題の次回出題日計算

### 9.2 Integration tests

- 初回起動 → 学年選択 → 10問 → 結果 → ホーム
- 100マス中断 → リロード → 再開 → 完了
- 英語誤答 → 同セット再出題 → 正解 → 翌日状態
- 旧Cookieあり → 起動 → XP・ランキング表示
- プレイ中にホーム → タイマーと音声停止
- 連打時にindexが2増えない

### 9.3 Content tests

- 全IDの一意性
- 学年、type、answer、tagsの必須項目
- 正解がdistractorsに含まれない
- 解説の空欄がない
- 不適切語・学年不相応な例文の目視レビュー記録

### 9.4 Manual QA

- 文字サイズ200%でも回答可能
- サウンドOFFが全画面で維持される
- Reduced Motionで紙吹雪と大きな動きが止まる
- VoiceOver / TalkBackで問題、選択肢、正誤、進捗を読める
- 色覚に依存せず正誤を判別できる
- PWAホーム追加後も更新できる

---

## 10. 受け入れ条件（ユーザーストーリー）

### 小学4年生

- 自分の学年を選び、説明なしで100マス掛け算を開始できる
- 間違えても答えを押し付けられず、短いヒントで再挑戦できる
- 1セット完了後、次に何をすればよいか分かる

### 小学5・6年生

- 小数・分数をスマートフォンで迷わず入力できる
- 約分前の正しい分数を不正解にされない
- 英単語の新規問題と苦手問題を混ぜて練習できる

### 中学1年生

- 正負の数、文字式、一次方程式を学年画面から選べる
- マイナス入力と方程式回答が片手で操作できる
- 小学生向け表示を幼すぎると感じても、操作と文言が学習を邪魔しない

### 保護者・先生

- アカウントなしで、直近の学習日、教科別進捗、苦手数を確認できる
- 公開順位や個人情報入力なしで利用できる
- 誤答回数と正答率を、速さとは別に確認できる

---

## 11. リスクと対策

| リスク | 影響 | 対策 |
|---|---|---|
| モード追加でホームが複雑化 | 子どもが開始できない | 教科4カードに固定し、詳細は次画面へ |
| `script.js` の全面改修 | 既存百人一首の回帰 | 純粋関数から段階抽出、各Phaseで回帰確認 |
| 英語教材の誤り | 学習品質低下 | データとロジックを分離し、レビュー状態を管理 |
| 100マスが小画面で読めない | 操作不能 | 現在問題を大表示、表は位置確認用にする |
| タイム競争が苦手意識を作る | 継続率低下 | 正確さ優先、比較対象は過去の自分 |
| Cookie容量超過 | 記録消失 | 新履歴をlocalStorageへ移行 |
| 端末時計操作で報酬重複 | ゲームバランス崩壊 | 最終記録日とclaimedを保存 |
| Service Workerが旧版を保持 | 修正が反映されない | バージョン更新と更新通知を標準手順化 |
| 中1にデザインが幼く見える | 離脱 | 絵は残しつつ、文言を過度に幼児化しない |

---

## 12. 初期リリースに含めないもの

- ログイン、クラウド同期、学校単位の管理画面
- オンライン対戦、公開ランキング、チャット
- 生成AIによる問題の自動生成
- 広告、課金、ガチャ、期間限定報酬
- 音声録音を使う発音採点
- 理科・社会の追加
- 先生向け問題作成ツール

これらは基礎コンテンツと復習ロジックの品質を確認した後に別計画とする。

---

## 13. Tera High への実装指示

1. Phase 0から順に進め、各Phaseの完了条件を満たすまで次へ進まない。
2. 作業開始前に `git status` と差分を確認し、既存の未コミット変更を上書きしない。
3. ルートの `index.html` / `script.js` / `style.css` を現行本体として扱う。`new/` の扱いは確認後に決める。
4. 既存Cookieのキー、ランキング、百人一首データを壊さない。
5. フレームワークや外部CDNを追加しない。オフライン動作と小さな配布サイズを維持する。
6. 教材内容とロジックを分離し、教材追加だけでアプリ本体を編集しなくてよい構造にする。
7. 1つの大きなコミットにせず、Phaseまたは機能単位で検証可能な変更にする。
8. 各Phaseの最後に、変更内容、テスト結果、残課題、次Phaseへの影響を報告する。
9. 不明点を独断で大規模変更しない。ただし、小さく可逆な判断は本書の設計原則に沿って進める。
10. 完了報告では、受け入れ条件を項目ごとに Pass / Fail / Deferred で示す。

### 最優先の実装順（短縮版）

```text
既存動作固定
→ 学年選択と新ホーム
→ 共通保存・結果・復習基盤
→ 100マス
→ 英語4択
→ 学年別算数・数学
→ ミッション・連続学習・コイン
→ 全体QAとPWA更新
```

この順序により、早い段階で「シンプル操作」「100マス」「英語」という主要価値を提供しつつ、教材量が増えても保守できる基盤を先に整える。
